# ESP32-C3 MQTT LED Controller

A simple IoT project that lets you turn an LED on an ESP32-C3 board on/off remotely
over MQTT. Each device identifies itself with a unique ID (derived from its MAC
address), announces itself on a registration topic when it boots, and listens for
ON/OFF commands, Wi-Fi management commands, and OTA update notifications on its own
topics. This lets you control multiple devices independently without any manual
per-device configuration.

This document is written for two audiences: a **quick summary** for non-technical
readers, and **full technical details** below for anyone maintaining or debugging
the system.

---

## For Non-Technical Readers

Think of each device as a lightbulb with a name tag. When you plug it in, it:

1. Connects to Wi-Fi (using credentials saved on the device).
2. Shouts out its name tag on a shared "roll call" channel so anyone listening
   knows it's alive.
3. Listens quietly on its own personal channels, waiting for someone to tell it
   "turn on", "turn off", or to change its Wi-Fi setup.
4. Remembers whatever it was last told, even if it loses power — it won't forget
   or reset to a random state.
5. If it can't find Wi-Fi, its light blinks instead of just glowing solid, so you
   can tell at a glance that it's having connection trouble rather than assuming
   it's broken.
6. **If Wi-Fi keeps failing, it starts advertising itself over Bluetooth and
   waits for the phone app.** The app shows nearby networks, you pick one and
   enter its password, and the device saves it and connects — no USB cable, no
   re-flashing needed. (This is the "provisioning" feature described below.)
7. While online it also reports health information — Wi-Fi signal strength,
   uptime and chip temperature — so the app can show how the device is doing.

To control a device, you (or any support tool) send a plain text message —
literally the word `ON` or `OFF` — to that device's personal channel. You don't
need to touch the device physically to control it once it's online.

If a device seems unreachable, the most common causes are: it lost Wi-Fi, its
Wi-Fi password was typed wrong, or (on this specific hardware) its antenna needs
a firm physical connection — see the [Known Hardware Issue](#known-hardware-issue-antenna)
section below.

---

## System Overview

```
[ESP32-C3 device] <---Wi-Fi---> [Router/AP] <---Internet---> [MQTT Broker] <---> [Phone app / control script]
```

- **Broker used for testing:** `test.mosquitto.org:1883` — a free, public, unencrypted
  Eclipse Mosquitto test server. No account or signup required. Suitable for
  development and testing only, not for production or sensitive data, since it's
  a shared public server with no authentication or privacy. The broker URI is
  **configurable via NVS** (see [MQTT broker configuration](#mqtt-broker-configuration)).
- **Firmware:** ESP-IDF (via PlatformIO), running on an ESP32-C3-DevKitM-1 board
  (or compatible ESP32-C3 boards, including "Super Mini" style modules). Uses
  NimBLE for provisioning and ESP-IDF's `mqtt`, `cJSON` and `esp_https_ota`
  components.
- **Control:** any MQTT client can publish `ON`/`OFF` to a device's topic. The
  companion Flutter app (repo `esp32c3-remote-app`) is the interactive client;
  this repo also ships Python helpers for OTA and partition validation.

---

## Repository Layout

| Path | Purpose |
|---|---|
| `src/main.c` | Core firmware: Wi-Fi, MQTT, LED, telemetry, OTA, management commands |
| `src/ble_prov.c` / `src/ble_prov.h` | BLE GATT provisioning service (NimBLE) |
| `src/CMakeLists.txt` / `src/idf_component.yml` | Component registration and managed dependencies |
| `include/version.h` | Generated by CI; **git-ignored** (local builds fall back to `"dev"`) |
| `platformio.ini` | Board/environment configuration (4 MB flash, custom partitions) |
| `partitions.csv` | OTA-enabled partition table (two 1700K app slots) |
| `sdkconfig.defaults` | Persisted Kconfig defaults (console, MQTT buffer, NimBLE, rollback) |
| `tools/publish_ota.py` | Publishes a retained OTA notification to `devices/ota` |
| `tools/validate_partitions.py` | Validates the partition table and firmware image size |
| `docs/` | In-depth architecture, API, deployment and development notes |
| `.github/workflows/` | `firmware-ci.yml` and `firmware-release.yml` |

---

## BLE Wi-Fi Provisioning

When the device cannot reach Wi-Fi — on first boot (no credentials are stored in
the firmware, so every new device starts here), when the saved password is wrong,
or when the [fallback window](#fallback-to-provisioning-policy) elapses — it
starts advertising a Bluetooth Low Energy service and waits for the phone app.
The LED keeps blinking the whole time so you can see it is in setup mode.

### GATT service

| Element | UUID |
|---|---|
| Service (Nordic UART Service style) | `6e400001-b5a3-f393-e0a9-e50e24dcca9e` |
| RX characteristic (phone → device, write) | `6e400002-b5a3-f393-e0a9-e50e24dcca9e` |
| TX characteristic (device → phone, notify) | `6e400003-b5a3-f393-e0a9-e50e24dcca9e` |

The BLE device advertises as `ESP32C3-<last 3 bytes of MAC>` (e.g.
`ESP32C3-a9e0f3`). The app connects, enables notifications on the TX
characteristic, then exchanges **one JSON message per line** (a message may span
several notifications; the phone buffers until it sees a `\n`). The phone writes
messages to RX; the device sends responses on TX.

### Phone → device messages

| Message | Purpose |
|---|---|
| `{"type":"scan"}` | Start a Wi-Fi scan. Results arrive as `scan_result` messages followed by a `scan_done` summary. |
| `{"type":"connect","ssid":"MyNetwork","password":"secret"}` | Save these credentials to NVS and connect. Device stops advertising BLE. |
| `{"type":"set_broker","url":"mqtt://host:1883"}` | Save a new MQTT broker URI to NVS. Takes effect after the next reboot. |

### Device → phone messages

| Message | Meaning |
|---|---|
| `{"type":"hello","device_id":"<id>","wifi":"provisioning"\|"connected"}` | Sent right after the phone subscribes to notifications. `<id>` is the 12-hex-char Wi-Fi MAC used for MQTT topics. |
| `{"type":"status","wifi":"...","detail":"..."}` | Status change. `wifi` is one of `provisioning`, `scanning`, `scan_error`, `connecting`, `error`, `connected`, `ok`. |
| `{"type":"scan_result","ssid":"...","rssi":-45,"auth":"WPA2","channel":6}` | One per access point found. `auth` is one of `OPEN`, `WEP`, `WPA`, `WPA2`, `WPA/WPA2`, `WPA2-ENT`, `WPA3`, `WPA2/WPA3`, `UNKNOWN`. |
| `{"type":"scan_done","count":N}` | Signals the end of a scan; the app should stop waiting. |

### Flow

1. Device has no stored credentials, or cannot connect within the fallback
   window → starts BLE advertising.
2. Phone scans, finds `ESP32C3-...`, connects, enables notifications, receives `hello`.
3. Phone sends `{"type":"scan"}`; device scans and streams `scan_result` lines, then `scan_done`.
4. Phone sends `{"type":"connect",...}` with the chosen network + password.
5. Device saves credentials to NVS, stops advertising, connects. On success the
   LED holds steady and MQTT starts; on repeated failure it re-enters
   provisioning mode once the fallback window elapses.

### Fallback to provisioning policy

If the device has stored credentials but cannot join the network within a
configurable window, it gives up retrying and switches to BLE provisioning. The
policy lives in NVS namespace `device_settings`:

| Key | Default | Range | Meaning |
|---|---|---|---|
| `fb_enabled` | `true` | – | Whether to fall back to provisioning at all |
| `fb_timeout` | `3` | 1–5 | Minutes without a connection before falling back |

The window can be changed at runtime with the `set_fallback` management command
(see [Wi-Fi management commands](#wi-fi-management-commands)).
A device with *no* credentials skips the wait and advertises immediately.

### Security note

This provisioning channel is **unencrypted** (no BLE pairing/bonding) for
simplicity in this first step. Anyone within radio range could connect while the
device is in provisioning mode. The device only advertises while it is
disconnected from Wi-Fi (or explicitly asked to re-provision), and stops
advertising as soon as it is provisioned, so the exposure window is limited. Add
pairing (SMP/encryption) before deploying this in a sensitive setting.

---

## MQTT Topics & Contracts

| Topic | Direction | Retained | Purpose |
|---|---|---|---|
| `devices/registry` | Device → broker | Yes | JSON announcement: device ID, command topic, LED state. |
| `devices/<device_id>/status` | Device → broker | Yes (online/offline); heartbeats not retained | JSON telemetry / online-offline state. Broker publishes the offline Last Will. |
| `devices/<device_id>/led/set` | App → device | No | `ON` or `OFF` (plain text, QoS 1). |
| `devices/<device_id>/wifi/set` | App → device | No | JSON Wi-Fi management commands (QoS 1). |
| `devices/ota` | Tool/backend → device | Yes | JSON firmware update notification (QoS 1). |

`<device_id>` is a 12-character hex string derived from the device's Wi-Fi MAC
address (e.g. `a1b2c3d4e5f6`), so every physical board gets a distinct, stable
identity automatically — the same firmware can be flashed to any number of
devices without editing anything per-unit.

### Registry payload (`devices/registry`)

Published on MQTT connect and re-published after every LED change, so clients
stay in sync without waiting for a reconnect:

```json
{
  "device_id": "a1b2c3d4e5f6",
  "status": "online",
  "led_topic": "devices/a1b2c3d4e5f6/led/set",
  "led_state": "OFF"
}
```

### Status & telemetry (`devices/<device_id>/status`)

On connect the device publishes a retained JSON status, then refreshes it every
30 seconds (heartbeats are not retained):

```json
{
  "state": "online",
  "device_id": "a1b2c3d4e5f6",
  "ssid": "MyNetwork",
  "rssi": -52,
  "uptime_s": 3600,
  "temp_c": 41.2
}
```

Special states, all retained:

| Payload | When |
|---|---|
| `{"state":"offline"}` | Last Will, set by the broker if the device disconnects unexpectedly. |
| `{"state":"offline","reason":"removed"}` | After a `remove` command (credentials erased). |
| `{"state":"online","reprovisioning":true}` | Acknowledgement of a `reprovision` command. |
| `{"state":"online","fallback_enabled":true,"fallback_timeout_min":3}` | Acknowledgement of a `set_fallback` command. |

### LED control (`devices/<device_id>/led/set`)

Plain text `ON` or `OFF`. Any other payload is logged and ignored. On a valid
command the device updates and persists the LED state and republishes its
registry entry.

### Wi-Fi management commands

Published to `devices/<device_id>/wifi/set` (QoS 1) as JSON, one command per
message:

| Command | Payload | Effect |
|---|---|---|
| Re-provision | `{"command":"reprovision"}` | Start BLE advertising so the app can set new Wi-Fi credentials. The device **stays online** on its current network until new credentials arrive. |
| Remove | `{"command":"remove"}` | Erase Wi-Fi credentials from NVS, blink, and advertise. The current Wi-Fi/MQTT session is kept alive until the device is re-provisioned, then it restarts. |
| Set fallback | `{"command":"set_fallback","enabled":true,"timeout_min":3}` | Persist and apply the fallback policy. `timeout_min` is clamped to 1–5. |

### OTA notification (`devices/ota`)

Retained JSON published by a tool or backend:

```json
{
  "version": "1.1.0",
  "url": "https://github.com/GLinBoy/esp32c3-firmware/releases/download/v1.1.0/firmware.bin",
  "sha256": "<64 hex characters>"
}
```

The device compares `version` with its own (`1.0.0 > 1.0.0-rc2 > 1.0.0-rc1`),
downloads newer images over HTTPS, verifies the SHA-256, and only then switches
partitions. See [OTA updates](#ota-updates).

---

## Firmware Behavior

### Startup sequence

1. Initialize LED GPIO, NVS, and the on-chip temperature sensor.
2. Restore the last-known LED state from NVS (survives power loss).
3. Build the device identity and topics from the Wi-Fi MAC.
4. Load Wi-Fi credentials, device settings (fallback policy), and the MQTT
   broker URI from NVS.
5. Initialize the BLE provisioning service (not advertising yet).
6. Start Wi-Fi. With stored credentials, connect and arm the fallback timer;
   without them, start BLE advertising immediately.
7. On obtaining an IP: stop blinking, apply the restored LED state, start the
   30-second heartbeat, and start the MQTT client.
8. On MQTT connect: subscribe to `led/set`, `wifi/set`, and `devices/ota`,
   publish the registration and status messages, check for a retained OTA
   notification, and mark a freshly updated image as valid (cancelling rollback).

### While disconnected from Wi-Fi

- The LED blinks continuously (approx. every 300ms) as a visual "trying to
  connect" indicator.
- Reconnection attempts use **exponential backoff**: 1s, 2s, 4s, 8s... up to a
  60-second maximum between attempts. This is intentional — retrying instantly
  and continuously was causing the device to run hot; backing off the retry
  rate significantly reduces power draw and heat during prolonged outages.
- If the fallback window expires first, retries stop and BLE provisioning begins.

### While connected

- The LED shows whatever state was last commanded (or restored from storage).
- Any `ON`/`OFF` message received on the device's topic immediately updates
  the LED, saves the new state to persistent storage, and republishes the
  registry entry.
- A status heartbeat with Wi-Fi health and temperature is published every 30s.

### OTA updates

- The device subscribes to `devices/ota` on every MQTT connect. A **retained**
  message serves both as a push notification and as the boot-time check, so no
  separate poll is needed.
- Version comparison is semver-aware, including pre-release suffixes
  (`1.0.0 > 1.0.0-rc2 > 1.0.0-rc1`).
- The download runs in a dedicated task via `esp_https_ota`, validating the
  server certificate against the ESP-IDF CA bundle and using enlarged HTTP
  buffers (GitHub release assets redirect to `objects.githubusercontent.com`).
- Because `esp_https_ota` exposes no raw-stream hash hook, the written image is
  read back and hashed; a SHA-256 mismatch **aborts the update and discards the
  image**, leaving the running firmware untouched.
- On success the device reboots into the new slot. Once it reaches the broker it
  calls `esp_ota_mark_app_valid_cancel_rollback()`, so a boot that can reach the
  network cancels the pending rollback (`CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE=y`).

### MQTT broker configuration

The broker URI is read at boot from NVS namespace `mqtt_config`, key
`broker_url`. If the key is absent or empty the compile-time default
(`MQTT_BROKER_URI_DEFAULT`, `mqtt://test.mosquitto.org:1883`) is used. It can be
changed over the BLE provisioning channel with
`{"type":"set_broker","url":"mqtt://host:1883"}`; the new URI takes effect on the
next reboot. This keeps the firmware ready for a migration to a private,
authenticated broker without a code change.

---

## Hardware Setup

- **Board:** ESP32-C3-DevKitM-1 (or compatible ESP32-C3 module)
- **LED:** connected to GPIO8 (`LED_GPIO` in the code) — this is commonly the
  onboard LED on many ESP32-C3 dev boards
- **LED polarity:** many onboard LEDs on these boards are **active-low**
  (GPIO output `0` = LED on, `1` = LED off). This is already configured
  correctly in the firmware (`LED_ON_LEVEL` / `LED_OFF_LEVEL`). If your specific
  board's LED behaves backwards, swap these two values.
- **Temperature sensor:** the on-chip sensor is used for the `temp_c` telemetry
  field. It is initialized at boot; no external hardware is required.

### Known Hardware Issue: Antenna

Some ESP32-C3 boards — particularly compact "Super Mini" style modules — have a
known PCB antenna design issue where the Wi-Fi connection is weak or fails
entirely unless something (like a finger) touches near the antenna to act as a
grounding/capacitive reference. Symptoms include:

- Device won't connect to Wi-Fi at all, or only connects intermittently.
- Touching the end of the board near the antenna suddenly makes it connect.
- Device runs unusually hot while repeatedly trying (and failing) to connect.

This is a documented hardware characteristic on some boards/batches, not a bug
in this firmware or a router misconfiguration. Mitigations, in order of
effectiveness:

1. **Software mitigation (included in this firmware):** reduced maximum Wi-Fi
   transmit power (`esp_wifi_set_max_tx_power(60)`, i.e. 15 dBm).
   Counterintuitively, lower power sometimes improves reliability on a poorly
   matched antenna, since an overdriven mismatched antenna can distort its own
   signal.
2. **Physical fix:** reposition or re-solder the antenna trace/connector
   slightly off the edge of the board, or attach a short wire extension to
   the antenna feed point. This has fully resolved the issue for many users
   on affected boards.
3. If neither helps, consider sourcing a different revision/batch of the same
   board, or a board with an external antenna connector (u.FL) instead of an
   onboard PCB antenna.

---

## Software Setup (PlatformIO + ESP-IDF)

### Project files

- `src/main.c` — firmware source (Wi-Fi + MQTT + provisioning + OTA + telemetry)
- `src/ble_prov.c` / `src/ble_prov.h` — BLE GATT provisioning service (NimBLE)
- `src/CMakeLists.txt` — declares component dependencies (`esp_wifi`, `esp_netif`,
  `esp_event`, `nvs_flash`, `driver`, `esp_driver_tsens`, `bt`)
- `src/idf_component.yml` — pulls `espressif/mqtt` (`^1.1.0`) and
  `espressif/cjson` (`^1.7.19`) from the ESP-IDF Component Registry
- `include/version.h` — `FIRMWARE_VERSION` header, **generated by CI** and
  git-ignored; local builds default to `"dev"`
- `platformio.ini` — board/environment configuration
- `partitions.csv` — OTA-enabled partition table for 4 MB-flash boards (two
  1700K app slots, `ota_0` + `ota_1`)
- `sdkconfig.defaults` — persisted configuration defaults (USB Serial/JTAG console,
  1024-byte MQTT buffer, NimBLE host/controller, rollback enabled, 4 MB flash,
  two-OTA-large table)
- `tools/publish_ota.py` — publishes an OTA notification to `devices/ota`
- `tools/validate_partitions.py` — validates `partitions.csv` and image size

### Before building

There are **no default Wi-Fi credentials in the source**. On first boot the
device has nothing to connect with, so it immediately enters BLE provisioning
mode and waits for the phone app (the LED blinks). Once you send it Wi-Fi
credentials through the app, they are saved to NVS and reused on every later
boot. To change the network later, use the app's "Change Wi-Fi" action (which
sends the `reprovision` command) or erase NVS.

### Build and flash

In VS Code with the PlatformIO extension installed, use **Upload and Monitor**
(or from a terminal in the project folder):

```bash
pio run --target fullclean   # clean rebuild, recommended after any config change
pio run --target upload
pio device monitor
```

> **Flash size:** this project targets **4 MB flash**, so it uses a custom
> OTA-capable partition table (`partitions.csv`, set via `board_build.partitions`
> in `platformio.ini`) with two 1700K app slots for A/B OTA updates. The board
> must be configured with `board_build.flash_size = 4MB`. If your board has less
> flash, you must adjust the partition table and flash size together.

### If the build fails with a missing config/component error

If you change `sdkconfig.defaults` and rebuild but nothing seems to change, the
per-environment generated config file (`sdkconfig.esp32-c3-devkitm-1`) may be
stale. Delete it and do a full clean rebuild:

```bash
rm sdkconfig.esp32-c3-devkitm-1
pio run --target fullclean
pio run
```

---

## Tools

The companion Flutter app is the interactive client for provisioning, monitoring
and control. Two Python helpers live in `tools/`:

### `tools/publish_ota.py`

Publishes a retained OTA notification. See [OTA updates](#ota-updates) for the
device behaviour.

```bash
python tools/publish_ota.py <version> <url> <sha256> [--broker HOST] [--port N] [--qos 0|1|2] [--no-retain]
```

Example:

```bash
python tools/publish_ota.py 1.1.0 \
  https://github.com/GLinBoy/esp32c3-firmware/releases/download/v1.1.0/firmware.bin \
  "$(cat firmware.sha256)"
```

Requires `paho-mqtt` (`pip install paho-mqtt`).

### `tools/validate_partitions.py`

Validates that every partition lies within the configured flash, that partitions
do not overlap, and (optionally) that `firmware.bin` fits in an app slot. Also
run automatically by CI.

```bash
python tools/validate_partitions.py --firmware .pio/build/esp32-c3-devkitm-1/firmware.bin
```

---

## Troubleshooting Guide

| Symptom | Likely Cause | Fix |
|---|---|---|
| No serial output after flashing | Console output routed to UART0 instead of USB | Set `CONFIG_ESP_CONSOLE_USB_SERIAL_JTAG=y` in `sdkconfig.defaults`, delete generated `sdkconfig.<env>` file, rebuild |
| Build fails: `mqtt_client.h: No such file or directory` | `mqtt` component not declared as a dependency, or built-in component is broken/empty | Declared via `src/idf_component.yml`; ensure managed components are downloaded (`pio run` fetches them). Do not ignore `dependencies.lock` or `src/idf_component.yml` |
| Wi-Fi connects then immediately disconnects, repeating (`auth -> init` / `assoc -> init` in logs) | Router/AP security or channel misconfiguration (seen with some MikroTik CAPsMAN setups), or weak/mismatched antenna | Check AP security profile (avoid mixed EAP+PSK, consider disabling PMF/management-protection), or address antenna issue above |
| LED behaves backwards (ON command turns it off, and vice versa) | LED wired active-low or active-high, opposite of firmware assumption | Swap `LED_ON_LEVEL` / `LED_OFF_LEVEL` values in `main.c` |
| LED is solid on/off while Wi-Fi is disconnected instead of blinking | Older firmware version without dedicated blink task | Use the current firmware version — includes a separate blink task that runs only while disconnected |
| LED forgets its state after a power cycle | Older firmware version without persistent storage | Use the current firmware version — LED state is saved to NVS on every change |
| Device enters BLE setup even though the Wi-Fi password is correct | It could not connect within the fallback window | Check logs / AP; adjust or disable the policy with `set_fallback`, or re-provision |
| Device runs hot, especially when it can't connect to Wi-Fi | Immediate/rapid reconnect retry loop, or antenna issue causing repeated failed high-power transmit attempts | Current firmware uses exponential backoff (1s → 60s max) between retries and reduced max TX power; also see antenna section above |
| OTA download fails with "Out of buffer" | Server redirect headers/paths exceed HTTP buffer defaults | Current firmware uses 4 KB RX/TX HTTP buffers; keep those when editing `perform_ota_update()` |
| Device only connects when touched near the antenna | Known PCB antenna design issue on some ESP32-C3 boards | See [Known Hardware Issue](#known-hardware-issue-antenna) section |

---

## Security Notes (Read Before Any Real-World Use)

This setup is intended for **prototyping and testing only**:

- `test.mosquitto.org` is a public, shared, unauthenticated broker. Anyone in
  the world can subscribe to the same topics if they guess or discover your
  topic names, and there is no encryption on the connection (`mqtt://`, not
  `mqtts://`).
- Wi-Fi credentials provisioned over BLE are stored in plain text in NVS
  (on-chip flash); they are not encrypted at rest.

Before using this in any production, home-security-relevant, or otherwise
sensitive context, you should:

- Move to a private broker (self-hosted Mosquitto, or a managed service like
  HiveMQ Cloud or EMQX Cloud) with username/password or certificate
  authentication. The broker URI already lives in NVS (set it over BLE with
  `set_broker`, or at build time via `MQTT_BROKER_URI_DEFAULT`).
- Use TLS (`mqtts://`, typically port 8883) instead of plain `mqtt://`.
- Avoid hardcoding Wi-Fi credentials in source control; use the built-in BLE
  provisioning flow so credentials live only in device NVS.

---

## CI/CD Pipeline

This project uses GitHub Actions for continuous integration and releases.

### Workflows

- **`firmware-ci.yml`**: on every push and on PRs to `main`, installs PlatformIO,
  generates a development version header (`dev-<short sha>`), builds, validates
  the partition table and image size, and uploads `firmware.bin` + `partitions.bin`
  as artifacts.
- **`firmware-release.yml`**: on tags matching `v*.*.*`, extracts the version,
  injects it into `include/version.h`, builds, validates, computes the SHA-256,
  and publishes a GitHub Release with `firmware.bin`, `partitions.bin`, and
  `firmware.sha256` (`softprops/action-gh-release@v2`).

Both workflows cache PlatformIO (`~/.platformio`) keyed on `platformio.ini` and
`dependencies.lock`.

### Creating a Release

```bash
git tag v1.1.0
git push origin v1.1.0
```

GitHub Actions will then:

1. Build firmware with version `1.1.0` injected
2. Validate the partition table / image size
3. Generate a SHA-256 checksum
4. Create a GitHub Release with `firmware.bin`, `partitions.bin`, and `firmware.sha256`

Pre-release tags (`v1.0.0-rc1`, …) are supported; the firmware's version
comparison understands pre-release ordering.

### OTA Updates

Devices automatically check for updates via the retained MQTT topic `devices/ota`:

- **Push notification**: publish update metadata to `devices/ota` (retained)
- **Boot-time check**: the retained message is delivered on every MQTT connect

To trigger OTA manually, publish the release URL and its checksum:

```bash
python tools/publish_ota.py 1.1.0 \
  https://github.com/GLinBoy/esp32c3-firmware/releases/download/v1.1.0/firmware.bin \
  <sha256>
```

**Important**: the MQTT broker URL is configurable via NVS; the default is
`mqtt://test.mosquitto.org:1883`. Use the BLE `set_broker` command (or change
`MQTT_BROKER_URI_DEFAULT`) for a private broker.

---

## Partition Table

**WARNING**: This project uses a custom OTA-enabled partition table
(`partitions.csv`) with two 1700K app slots (`ota_0` and `ota_1`), an `otadata`
partition and `nvs`:

```
nvs,      data, nvs,     0x9000,  0x6000
otadata,  data, ota,     0xf000,  0x2000
phy_init, data, phy,     0x11000, 0x1000
ota_0,    app,  ota_0,   0x20000, 1700K
ota_1,    app,  ota_1,           , 1700K
```

If you modify the partition table:

1. Delete `.pio/build` directory
2. Delete generated `sdkconfig` file
3. Run `pio run --target fullclean`
4. Rebuild: `pio run`
5. Validate: `python tools/validate_partitions.py --firmware .pio/build/esp32-c3-devkitm-1/firmware.bin`

Incremental builds do NOT reliably pick up partition table changes and will cause
silent boot failures.

---

## Further Documentation

The `docs/` directory contains deeper, continuously-maintained references:

| Document | Contents |
|---|---|
| [`docs/system-architecture.md`](docs/system-architecture.md) | Full ecosystem architecture (firmware, app, backend, Matter roadmap) |
| [`docs/api-reference.md`](docs/api-reference.md) | Backend/cloud API reference for the future phases |
| [`docs/deployment-guide.md`](docs/deployment-guide.md) | Build, flash and provisioning procedures |
| [`docs/architecture-decision-records.md`](docs/architecture-decision-records.md) | Rationale behind key design choices |
| [`docs/ble-provisioning-development-notes.md`](docs/ble-provisioning-development-notes.md) | BLE provisioning implementation notes and gotchas |

---

## Glossary (for non-technical readers)

- **MQTT** — a lightweight messaging protocol where devices "publish" short
  messages to named "topics," and other devices/programs "subscribe" to those
  topics to receive them. Think of it like a set of labeled radio channels.
- **Broker** — the server that routes MQTT messages between publishers and
  subscribers. Devices and control tools both connect to the same broker.
- **Topic** — a named channel, e.g. `devices/a1b2c3d4e5f6/led/set`.
- **Retained message** — a message the broker remembers and immediately
  re-sends to any new subscriber, even if they connect after it was
  originally sent.
- **Last Will** — a message the broker stores and publishes on a device's behalf
  if the device disconnects unexpectedly (used for the `offline` status).
- **OTA** — "over-the-air" update: replacing the firmware on the device without
  a USB cable.
- **NVS (Non-Volatile Storage)** — a small persistent storage area on the
  chip that keeps its contents even when power is removed, used here to
  remember the LED's last commanded state, Wi-Fi credentials, device settings,
  and the broker URI.
- **GPIO** — General Purpose Input/Output; a physical pin on the chip that
  can be set high or low to control something like an LED, or read a sensor.
